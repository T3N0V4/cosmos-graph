{
  "name": "cosmos-graph",
  "version": "alpha-1.0.0",
  "description": "Cosmic animated background for Obsidian Graph View",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild src/main.ts --bundle --external:obsidian --format=cjs --watch --outfile=main.js",
    "build": "esbuild src/main.ts --bundle --external:obsidian --format=cjs --outfile=main.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.2",
    "obsidian": "latest",
    "typescript": "^5.4.2"
  },
  "dependencies": {
    "tslib": "^2.8.1"
  }
}
